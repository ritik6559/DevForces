apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: devforces-cluster
  region: ap-south-1
  version: "1.31"

iam:
  withOIDC: true

managedNodeGroups:
  - name: standard-nodes
    instanceType: t3.medium
    desiredCapacity: 3
    volumeSize: 20
